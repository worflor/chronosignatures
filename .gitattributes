# Normalize text files to LF in the repo regardless of platform checkout settings
* text=auto eol=lf

# Binary assets — never diff/merge as text
*.wav   binary
*.mp3   binary
*.mid   binary
*.jpg   binary
*.jpeg  binary
*.png   binary
